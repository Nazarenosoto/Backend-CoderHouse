<div>
    <h2>Product List</h2>
    {{#each productos}}
        <div>
            <h3>{{this.title}}</h3>
            <p>{{this.description}}</p>
            <p>{{this.price}}</p>
            <p>{{this.stock}}</p>
        </div>
    {{/each}}
</div>